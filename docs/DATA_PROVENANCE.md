# Data provenance

The public repository contains compact, versioned derivatives selected from
the local malonaldehyde project. Local source paths remain only in the private
Stage65 build report, not in the Git-tracked public manifest.

No new scientific calculation is performed during repository preparation.
